<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Outer rectangle -->
    <item android:left="5dp" android:right="5dp" android:top="5dp" android:bottom="5dp">
        <shape android:shape="rectangle">
            <stroke
                android:width="2dp"
                android:color="#000000" /> <!-- Set the color of the rectangle border -->
            <solid android:color="@android:color/transparent" /> <!-- Transparent background -->
        </shape>
    </item>
    <!-- Dotted pattern -->
    <item android:left="10dp" android:right="10dp" android:top="10dp" android:bottom="10dp">
        <shape android:shape="rectangle">
            <stroke
                android:width="1dp"
                android:color="#000000" /> <!-- Set the color of the dotted lines -->
            <size android:height="1dp" /> <!-- Set the height of the dotted lines -->
<!--            <dash android:gap="2dp" android:width="2dp" /> &lt;!&ndash; Set the dash pattern &ndash;&gt;-->
            <solid android:color="@android:color/transparent" /> <!-- Transparent background -->
        </shape>
    </item>
</layer-list>
